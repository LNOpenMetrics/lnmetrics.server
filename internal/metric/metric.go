package metric

import (
	"github.com/LNOpenMetrics/lnmetrics.server/graph/model"
)

// Method to calculate the metric one output and store the result
// on the server
func CalculateMetricOneOutput(metricModel *model.MetricOne) error {
	// TODO split the call across different services
	return nil
}
